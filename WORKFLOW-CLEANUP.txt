LITTLE REALM - GITHUB ACTIONS CLEANUP

The canonical Pages workflow is:
  .github/workflows/static.yml

After copying this build into the repository, open:
  .github/workflows/

Delete every OTHER .yml or .yaml workflow file that belongs to the old Little Realm Pages deployment.
The warning you were seeing explicitly referenced actions/checkout@v4 and actions/upload-artifact@v4; those do not appear in the canonical static.yml in this build.

Then commit/push the deletion and replacement. The next Actions run should use only the canonical workflow.

Optional local check:
  python tools/workflow_audit.py

It will fail and name the exact workflow file if an old action reference still exists.
